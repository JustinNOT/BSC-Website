# Only track bsc-vcm; ignore this nested folder
Fourth Stage - Redemption/

# Web app (models/*.joblib are committed so Railway can deploy them)
frontend/node_modules
.env
.env.local
.env.production
frontend/.env.production
__pycache__/
*.pyc
